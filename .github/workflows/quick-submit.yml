name: Quick Submit (disabled)

on:
  workflow_dispatch:

jobs:
  quick-submit-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain why this workflow is disabled
        run: |
          echo "This local quick-submit workflow is disabled."
          echo "Reason: the old reusable workflow reference is no longer valid:"
          echo "agentbeats/agentbeats/.github/workflows/quick-submit.yml@main"
          echo ""
          echo "Use the official leaderboard quick-submit flow instead."
          exit 1
